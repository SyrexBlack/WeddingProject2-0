---
phase: 02-static-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/InfoSection.tsx
  - src/components/sections/DresscodeSection.tsx
  - src/components/sections/FooterSection.tsx
  - src/app/page.tsx
autonomous: true
requirements: [CONT-01, CONT-03, CONT-04, CONT-07]

must_haves:
  truths:
    - "Hero section displays couple names, invitation phrase, and date centered on full-screen with glassmorphism overlay"
    - "Info section shows 'Когда' and 'Где' cards stacked vertically with large Lucide icons above each title"
    - "Dresscode section shows description text and 5 color swatches at 56-64px diameter with names below"
    - "Footer shows 'Анна & Михаил · 2026' with gradient top border and muted appearance"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Full-screen hero with glassmorphism backdrop-blur overlay"
      contains: "heroData"
    - path: "src/components/sections/InfoSection.tsx"
      provides: "Info cards stacked vertically with Lucide Calendar/MapPin icons"
      contains: "infoCards"
    - path: "src/components/sections/DresscodeSection.tsx"
      provides: "Dress code text and large color palette circles"
      contains: "dressCodeData"
    - path: "src/components/sections/FooterSection.tsx"
      provides: "Minimal footer with gradient line and copyright"
      contains: "footerData"
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "@/lib/constants"
      via: "import heroData"
      pattern: "import.*heroData.*from.*constants"
    - from: "src/components/sections/InfoSection.tsx"
      to: "@/lib/constants"
      via: "import infoCards"
      pattern: "import.*infoCards.*from.*constants"
    - from: "src/app/page.tsx"
      to: "src/components/sections/*"
      via: "imports all 4 section components"
      pattern: "import.*from.*components/sections"
---

<objective>
Create the 4 simpler static section components (Hero, Info, Dresscode, Footer) as dedicated files and integrate them into page.tsx, replacing Phase 1 placeholder markup.

Purpose: Deliver the visual content sections that display wedding data from constants.ts with the design decisions from CONTEXT.md (glassmorphism hero, single-column info cards, large dresscode swatches, minimal footer).
Output: 4 section component files + updated page.tsx using them.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-sections/02-CONTEXT.md
@src/lib/types.ts
@src/lib/constants.ts
@src/app/page.tsx
@src/components/ui/Container.tsx
@src/components/ui/Card.tsx
@src/components/ui/SectionHeading.tsx
@src/components/ui/AnimatedSection.tsx
@tailwind.config.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection, InfoSection, DresscodeSection, FooterSection components</name>
  <files>
    src/components/sections/HeroSection.tsx
    src/components/sections/InfoSection.tsx
    src/components/sections/DresscodeSection.tsx
    src/components/sections/FooterSection.tsx
  </files>
  <action>
Create `src/components/sections/` directory and 4 component files. All are server components (no 'use client' needed — no browser APIs, no state).

**HeroSection.tsx** (CONT-01):
- Import `heroData` from `@/lib/constants`
- Full-screen section: `id="hero"`, `className="h-screen relative flex items-center justify-center"`
- Background: use the existing SVG feTurbulence paper texture from `globals.css` as the page background — the hero section just needs a slightly different shade or an overlay. Per CONTEXT.md: "Без реального фото — фон из SVG feTurbulence текстуры бумаги (уже есть в проекте)". Add a subtle `bg-chocolate/5` or similar background tint to differentiate hero from the rest.
- Glassmorphism overlay: centered `div` with `backdrop-blur-md bg-white/30 rounded-2xl p-8 sm:p-12 shadow-lg border border-white/20` for the glassmorphism effect described in CONTEXT.md.
- Content ORDER (top to bottom per CONTEXT.md): **names → phrase → date**
  - Names: `heroData.coupleNames` — large heading (`text-5xl sm:text-7xl font-calmius text-chocolate`)
  - Invitation phrase: `heroData.invitationPhrase` — medium text (`text-lg sm:text-xl opacity-80 mt-4`)
  - Date: `heroData.date` — below phrase (`text-xl sm:text-2xl mt-6 text-alexandrite font-medium`)
- NO scroll indicator (per CONTEXT.md: "Без скролл-индикатора")
- Export as named export: `export function HeroSection()`

**InfoSection.tsx** (CONT-03):
- Import `infoCards` from `@/lib/constants`, `Container`, `SectionHeading`, `AnimatedSection`, `Card` from UI
- Import `Calendar`, `MapPin` from `lucide-react`
- Wrap in `AnimatedSection` with `id="info"` and `className="py-16"`
- Use `SectionHeading` with text "О торжестве" (from `sectionOrder` constant)
- Per CONTEXT.md: **single column, stacked vertically** — NOT grid. Use `div` with `space-y-6 mt-8`
- Map over `infoCards`. For each card, render inside a `Card` component:
  - **Large centered Lucide icon above title**: icon size 40-48px, color alexandrite, `mx-auto mb-4`
  - Map icon by `card.type`: `'when'` → `Calendar`, `'where'` → `MapPin`
  - Title: `card.title` centered, `text-xl font-medium text-center mb-2`
  - Lines: `card.lines` mapped to `<p>` tags, centered, `text-center opacity-80`
- Export as named export: `export function InfoSection()`

**DresscodeSection.tsx** (CONT-04):
- Import `dressCodeData` from `@/lib/constants`, `Container`, `SectionHeading`, `AnimatedSection`
- Wrap in `AnimatedSection` with `id="dresscode"` and `className="py-16"`
- Use `SectionHeading` with text "Дресс-код"
- Per CONTEXT.md: **only text + palette, no icons/illustrations**
- Description text: `dressCodeData.description` — `text-center mt-6 mb-8 text-lg`
- Color swatches: `flex justify-center gap-6 flex-wrap`
  - Per CONTEXT.md: **large circles 56-64px** — use `w-14 h-14 sm:w-16 sm:h-16` (56px/64px)
  - Each swatch: `rounded-full mx-auto mb-2 shadow-sm border border-white/50`
  - Label below: `swatch.name` in `text-sm opacity-70 text-center`
- Export as named export: `export function DresscodeSection()`

**FooterSection.tsx** (CONT-07):
- Import `footerData` from `@/lib/constants`
- Per CONTEXT.md decisions:
  - **Only copyright**: "Анна & Михаил · 2026" from `footerData.text`
  - **Gradient line on top**: alexandrite gradient matching SectionHeading style — `linear-gradient(to right, transparent, rgba(89, 140, 116, 0.4), transparent)` as a 1px height div
  - **Muted text**: `text-sm opacity-50`
  - **Slightly different background**: `bg-cream-dark` or `bg-chocolate/3` — subtle contrast vs main bg
  - **Compact padding**: `py-4 sm:py-6`
  - **No icons**, no "Made with", no contacts
- Use `<footer>` tag with `id="footer"`
- Export as named export: `export function FooterSection()`
  </action>
  <verify>
All 4 files exist in `src/components/sections/`. Each imports from `@/lib/constants` and uses correct data. `npm run build` passes without errors.
  </verify>
  <done>
HeroSection renders glassmorphism overlay with names → phrase → date order. InfoSection renders stacked cards with large icons. DresscodeSection renders text and 56-64px color circles. FooterSection renders gradient top line and muted copyright.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate section components into page.tsx</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Update `src/app/page.tsx` to import and use the 4 new section components, replacing the Phase 1 placeholder markup for hero, info, dresscode, and footer sections.

- Import `HeroSection` from `@/components/sections/HeroSection`
- Import `InfoSection` from `@/components/sections/InfoSection`
- Import `DresscodeSection` from `@/components/sections/DresscodeSection`
- Import `FooterSection` from `@/components/sections/FooterSection`

Replace:
1. The `<section id="hero" ...>` block → `<HeroSection />`
2. The `<AnimatedSection id="info" ...>` block → `<InfoSection />`
3. The `<AnimatedSection id="dresscode" ...>` block → `<DresscodeSection />`
4. The `<footer id="footer" ...>` block → `<FooterSection />`

Keep UNTOUCHED (still placeholders for later phases):
- Countdown section (Phase 3)
- RSVP section (Phase 4)
- Map section (Phase 4)
- Timeline section (Phase 2 Plan 02)

Remove unused imports that were only needed for replaced sections (e.g., `heroData`, `dressCodeData`, `footerData`, `infoCards` — but keep `timelineData`, `countdownData`, `sectionOrder` if still used). Keep `Container`, `Button`, `Card`, `SectionHeading`, `AnimatedSection` imports if still used by remaining placeholder sections.

Ensure `SectionDots` import stays at top. Section order in JSX remains: Hero, Countdown, Info, Timeline, Dresscode, RSVP, Map, Footer.
  </action>
  <verify>
`npm run build` passes. `npm run dev` shows the page with new section components rendered. No console errors. Section IDs preserved for SectionDots navigation.
  </verify>
  <done>
page.tsx uses HeroSection, InfoSection, DresscodeSection, FooterSection components. Placeholder sections (countdown, timeline, RSVP, map) remain unchanged. Build passes with zero errors.
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- All section IDs (hero, countdown, info, timeline, dresscode, rsvp, map, footer) still present in rendered HTML for SectionDots navigation
- Each section component imports data from `@/lib/constants`
- No hardcoded strings that should come from constants
</verification>

<success_criteria>
- Hero shows glassmorphism overlay with names/phrase/date from heroData
- Info shows stacked cards with Calendar/MapPin icons from infoCards
- Dresscode shows description + 56-64px color circles from dressCodeData
- Footer shows gradient line + muted copyright from footerData
- Build passes, all section IDs preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-01-SUMMARY.md`
</output>
