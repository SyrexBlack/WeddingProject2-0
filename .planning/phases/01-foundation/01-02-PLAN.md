---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/constants.ts
  - src/components/ui/Container.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/SectionHeading.tsx
  - src/components/ui/AnimatedSection.tsx
autonomous: true
requirements:
  - FOUND-03
  - FOUND-05

must_haves:
  truths:
    - "constants.ts содержит все данные свадьбы (имена, дата, место, программа, дресс-код) с полной типизацией"
    - "Базовые UI-компоненты (Container, Button, Card, AnimatedSection, SectionHeading) рендерятся без ошибок"
    - "Данные экспортируются как отдельные именованные экспорты по секциям (heroData, timelineData и т.д.)"
    - "Button имеет два стиля (outline + filled) и два размера (normal + large)"
    - "SectionHeading показывает текст с декоративными линиями по бокам"
    - "AnimatedSection оборачивает контент и добавляет fade-анимацию при скролле"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "All wedding data with full typing"
      exports: ["heroData", "countdownData", "infoCards", "timelineData", "dressCodeData", "rsvpConfig", "mapData", "footerData", "sectionOrder"]
    - path: "src/components/ui/Container.tsx"
      provides: "Centered content container with max-width"
      exports: ["Container"]
    - path: "src/components/ui/Button.tsx"
      provides: "Button with outline/filled variants and normal/large sizes"
      exports: ["Button"]
    - path: "src/components/ui/Card.tsx"
      provides: "Card with subtle shadow and moderate rounding"
      exports: ["Card"]
    - path: "src/components/ui/SectionHeading.tsx"
      provides: "Section heading with decorative alexandrite side lines"
      exports: ["SectionHeading"]
    - path: "src/components/ui/AnimatedSection.tsx"
      provides: "Framer Motion wrapper for scroll-triggered animations"
      exports: ["AnimatedSection"]
  key_links:
    - from: "src/lib/constants.ts"
      to: "src/lib/types.ts"
      via: "type imports for all data structures"
      pattern: "import.*from.*types"
    - from: "src/components/ui/AnimatedSection.tsx"
      to: "framer-motion"
      via: "motion component import"
      pattern: "from.*framer-motion"
---

<objective>
Create the wedding data constants file with all typed content, and build the base UI component library (Container, Button, Card, SectionHeading, AnimatedSection) matching the locked visual decisions from CONTEXT.md.

Purpose: Provide the data layer and reusable components that all content sections (Phase 2+) will consume.
Output: Fully typed constants.ts with wedding data, 5 styled UI components ready for composition.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants.ts with all typed wedding data</name>
  <files>
    src/lib/constants.ts
  </files>
  <action>
    Create `src/lib/constants.ts` with typed wedding data. Per CONTEXT.md decisions:
    - **Separate named exports per section** (not one big object): `heroData`, `countdownData`, `infoCards`, `timelineData`, `dressCodeData`, `rsvpConfig`, `mapData`, `footerData`
    - **Real placeholder data** — use realistic Russian text, not lorem ipsum
    - **Язык: только русский**
    - **Timezone: Europe/Moscow** (захардкожено)
    - Import all types from `@/lib/types`

    ```ts
    import type { HeroData, CountdownData, InfoCard, TimelineEvent, DressCodeData, ContactInfo, MapData, FooterData, SectionId, SectionConfig } from '@/lib/types';

    // Hero — per decisions: имена (только имена, амперсанд), дата прописью, поэтичная фраза
    export const heroData: HeroData = {
      coupleNames: 'Анна & Михаил',
      date: '15 августа 2026',
      invitationPhrase: 'Приглашаем вас разделить с нами этот особенный день', // Claude's discretion: warm, poetic
      backgroundImageDesktop: '/images/hero-desktop.jpg',
      backgroundImageMobile: '/images/hero-mobile.jpg',
    };

    // Countdown — per decisions: дата ISO, timezone Moscow, сообщение после свадьбы
    export const countdownData: CountdownData = {
      weddingDate: '2026-08-15T15:00:00+03:00', // 15:00 Moscow time
      timezone: 'Europe/Moscow',
      expiredMessage: 'Спасибо, что были с нами!',
    };

    // Info cards — per decisions: два: "Когда" и "Где", одно место
    export const infoCards: InfoCard[] = [
      {
        type: 'when',
        title: 'Когда',
        lines: ['15 августа 2026', 'Сбор гостей в 15:00'],
      },
      {
        type: 'where',
        title: 'Где',
        lines: ['Усадьба «Архангельское»', 'Московская область, Красногорский район'],
      },
    ];

    // Timeline — per decisions: ~5 этапов, время + название, без описания
    export const timelineData: TimelineEvent[] = [
      { time: '15:00', title: 'Сбор гостей' },
      { time: '16:00', title: 'Церемония' },
      { time: '17:00', title: 'Банкет' },
      { time: '20:00', title: 'Свадебный торт' },
      { time: '22:00', title: 'Завершение вечера' },
    ];

    // Dress code — per decisions: короткая фраза + 4-5 кружков палитры
    export const dressCodeData: DressCodeData = {
      description: 'Мы будем рады видеть вас в нарядах пастельных и природных оттенков',
      palette: [
        { color: '#598c74', name: 'Александрит' },
        { color: '#D4C5A9', name: 'Шампань' },
        { color: '#C9B99A', name: 'Песочный' },
        { color: '#E8D5C4', name: 'Пудровый' },
        { color: '#B8C5B2', name: 'Шалфей' },
      ],
    };

    // RSVP config — contact info for error state per decisions
    export const rsvpConfig: { contact: ContactInfo } = {
      contact: {
        phone: '+7 (999) 123-45-67',
        telegramUrl: 'https://t.me/example',
        whatsappUrl: 'https://wa.me/79991234567',
      },
    };

    // Map — per decisions: iframe Яндекс.Карт, кнопка маршрута, адрес
    export const mapData: MapData = {
      iframeSrc: 'https://yandex.ru/map-widget/v1/?ll=37.284722%2C55.784444&z=15&l=map&pt=37.284722%2C55.784444%2Cpm2rdm',
      address: 'Усадьба «Архангельское», Московская область, Красногорский район',
      coordinates: { lat: 55.784444, lng: 37.284722 },
      navigationUrl: 'https://yandex.ru/maps/?rtext=~55.784444%2C37.284722&rtt=auto',
    };

    // Footer — per decisions: минимальный "Имена · Год"
    export const footerData: FooterData = {
      text: 'Анна & Михаил · 2026',
    };

    // Section order — per decisions (8 sections in specific order)
    export const sectionOrder: SectionConfig[] = [
      { id: 'hero', title: null },
      { id: 'countdown', title: null },          // без заголовка per decisions
      { id: 'info', title: 'О торжестве' },
      { id: 'timeline', title: 'Как пройдёт наш день' },
      { id: 'dresscode', title: 'Дресс-код' },
      { id: 'rsvp', title: 'Ждём вашего ответа' },
      { id: 'map', title: 'Место проведения' },
      { id: 'footer', title: null },
    ];
    ```

    All data must be placeholder-realistic. User will replace with real data when available (per STATE.md pending tasks).
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors. All constants must satisfy their type interfaces. Verify each export is accessible: `npm run build` passes.
  </verify>
  <done>
    constants.ts has all wedding data with full TypeScript typing. Separate named exports: heroData, countdownData, infoCards, timelineData, dressCodeData, rsvpConfig, mapData, footerData, sectionOrder. All realistic Russian placeholder content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build base UI components (Container, Button, Card, SectionHeading, AnimatedSection)</name>
  <files>
    src/components/ui/Container.tsx
    src/components/ui/Button.tsx
    src/components/ui/Card.tsx
    src/components/ui/SectionHeading.tsx
    src/components/ui/AnimatedSection.tsx
  </files>
  <action>
    Install Framer Motion: `npm install framer-motion@latest`

    Install Lucide React (needed for icons in future phases, and to verify thin strokeWidth): `npm install lucide-react@latest`

    Create 5 UI components in `src/components/ui/`. All must follow CONTEXT.md locked decisions exactly:

    **1. Container.tsx** — Centered content wrapper
    ```tsx
    // Props: children, className (optional), as (optional element type, default 'div')
    // Applies: max-w-content (768px per decisions), mx-auto, px-4 sm:px-6
    // Simple wrapper, no visual styling beyond centering
    ```

    **2. Button.tsx** — Per decisions: два стиля (outline + filled), два размера (normal + large)
    ```tsx
    // Props: children, variant ('outline' | 'filled'), size ('normal' | 'large'),
    //        className?, onClick?, disabled?, type?, href? (renders <a> if href provided)
    //
    // Outline (default, основной стиль per decisions):
    //   - Border + text in alexandrite (#598c74)
    //   - On hover: smooth fill with alexandrite, text turns white
    //   - Transition: ~300ms ease
    //
    // Filled (для CTA / RSVP "Подтвердить" per decisions):
    //   - Background alexandrite, white text
    //   - On hover: slightly darker alexandrite
    //
    // Sizes: normal (py-2 px-6 text-base), large (py-3 px-8 text-lg)
    // Border radius: rounded-card (10px per decisions)
    // Disabled state: opacity-50 cursor-not-allowed
    // Focus: outline ring in alexandrite
    ```

    **3. Card.tsx** — Per decisions: умеренное скругление, едва заметные тени
    ```tsx
    // Props: children, className?
    // Styling: bg-white/80 (slight transparency), rounded-card (10px),
    //          shadow-sm (едва заметная тень per decisions), p-6
    // Keep simple — sections will compose with this
    ```

    **4. SectionHeading.tsx** — Per decisions: текст + декоративные линии по бокам с утончением к краям
    ```tsx
    // Props: children (heading text), className?
    // Layout: flex items-center gap-4
    // Lines: decorative lines on both sides that taper/thin toward edges
    //   - Use gradient: from transparent → alexandrite-light → transparent
    //   - Or use CSS border with gradient via pseudo-elements
    //   - Height: 1px, flex-grow on both sides
    //   - Color: светлый александрит per decisions
    // Text: text-2xl sm:text-3xl, text-chocolate, font-calmius, whitespace-nowrap
    // Result: ——— Заголовок ——— with elegant tapered lines
    ```

    **5. AnimatedSection.tsx** — Per decisions: Framer Motion, fade-up default, multiple variants via prop, slow ~0.8-1s, triggers every scroll
    ```tsx
    'use client'; // Client component for Framer Motion

    // Props: children, variant ('fade-up' | 'fade-in' | 'fade-left' | 'fade-right'),
    //        className?, delay? (number), id? (for section anchoring)
    //
    // Uses framer-motion's motion.div with:
    //   initial: depends on variant:
    //     fade-up: { opacity: 0, y: 30 }
    //     fade-in: { opacity: 0 }
    //     fade-left: { opacity: 0, x: -30 }
    //     fade-right: { opacity: 0, x: 30 }
    //   whileInView: { opacity: 1, y: 0, x: 0 }
    //   transition: { duration: 0.9, ease: 'easeOut', delay: delay || 0 }
    //   viewport: { once: false, amount: 0.15 }
    //     - once: false per decisions (срабатывают каждый раз)
    //     - amount: 0.15 per decisions (порог ~10-20%)
    //
    // Default variant: 'fade-up' per decisions (основной вариант)
    ```

    All components must be properly typed with React.FC or function component patterns. Use Tailwind classes exclusively (no inline styles except where CSS gradients needed). Export each as named export.
  </action>
  <verify>
    Run `npm run build` — zero TypeScript errors. Verify Framer Motion and Lucide React are in package.json dependencies. Create a temporary test in page.tsx that renders each component to verify no runtime errors (can be cleaned up in Plan 03).
  </verify>
  <done>
    5 UI components created and typed: Container (centered wrapper), Button (outline/filled, normal/large), Card (subtle shadow), SectionHeading (text with tapered alexandrite lines), AnimatedSection (Framer Motion scroll-triggered with 4 variants). All follow CONTEXT.md visual decisions. `npm run build` passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. All constants are importable: `import { heroData, timelineData, ... } from '@/lib/constants'`
3. All components are importable: `import { Container, Button, Card, SectionHeading, AnimatedSection } from '@/components/ui/...'`
4. Button renders in both outline and filled variants with correct colors
5. SectionHeading shows decorative tapered lines in alexandrite
6. AnimatedSection triggers fade animation on scroll
7. Framer Motion and Lucide React installed
</verification>

<success_criteria>
- constants.ts exports all wedding data with correct types (heroData, countdownData, infoCards, timelineData, dressCodeData, rsvpConfig, mapData, footerData, sectionOrder)
- 5 UI components created, typed, and rendering: Container, Button, Card, SectionHeading, AnimatedSection
- Visual styles match CONTEXT.md decisions: alexandrite accent, outline/filled buttons, moderate rounding, subtle shadows, tapered heading lines
- AnimatedSection supports 4 variants (fade-up, fade-in, fade-left, fade-right) with slow timing (~0.9s) and repeat-on-scroll
- `npm run build` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
